// Base URL of your deployed Second Brain Next.js app.
// Replace the value below with your actual production URL before building.
// Examples:
//   https://your-app.vercel.app          (Vercel deployment)
//   http://10.0.2.2:3000                 (Android emulator → local dev server)
//   http://localhost:3000                (iOS simulator → local dev server)
//
// Note: when using QR code pairing the server URL is read automatically from
// the QR code, so you only need to set this for direct email/password login.
export const API_BASE_URL = 'https://REPLACE-WITH-YOUR-DEPLOYED-URL.vercel.app'
